version: "2"
run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - bodyclose
    - noctx
    - contextcheck
    - gosec
  settings:
    staticcheck:
      checks: ["all"]

formatters:
  enable:
    - gofmt